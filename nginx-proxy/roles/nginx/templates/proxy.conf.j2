upstream customer {
    {% for server in groups['{{ backend_group }}'] %}
        server {{ hostvars[server]['ansible_eth0']['ipv4']['address'] }}:{{ backend_port }};
    {% endfor %}

}

server {
    listen {{ listen_port }};
    port_in_redirect off;

    location / {
        proxy_pass http://customer;
    }
}
