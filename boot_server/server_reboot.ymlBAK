- name: Reboot a server
  connection: local
  hosts: all
  tasks:
    - name: Server stop
      clc_server:
        server_ids: "{{ servers }}"
        state: stopped

    - name: Server start
      clc_server:
        server_ids: "{{ servers }}"
        state: started
